//to find the length of string(.length)
var a="muniyappa"
console.log(a.length)


//to find the character at(var.charAt)
console.log(a.charAt(0))

//to find the index of a string(var.indexOf)
var b="chandan"
console.log(b.indexOf('a'))


//to find the last indexof of a string(var.lastindexOf)
var c="muniyappa"
console.log(c.lastIndexOf('p'))

//substr:to display the string from where we need by giving index values

//substring:it display the value from starting index to ,last index's previous value

// slice:same as sub string
var d="darshan"
console.log(d.substr(2,3))
console.log(d.substring(3,6))
console.log(d.slice(3,6))

//includes:to check the word is present in a given string or not
var e="muniyappa"
console.log(e.includes('u'))

//cocat:adding of two strings
var a="chirag"
var b="muniyappa"
console.log(a.concat(b))

